{
  "name": "3d-file-viewer",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "cd client && yarn start && cd ..",
    "build": "cd client && yarn && yarn run build && cd .."
  },
  "dependencies": {}
}
